"use strict";

import Radium from "radium";

class AssignStatement extends React.Component {

  render() {

    const styles = {
      root: {
      }
    };

    return (
      <div style={styles.root}>

      </div>
    );
  }
}

export default Radium(AssignStatement);
